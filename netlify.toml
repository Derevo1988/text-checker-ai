[build]
  functions = "netlify/functions"
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"

# Настройка таймаута для конкретной функции
[[functions]]
  pattern = "check-text"
  timeout = 30
